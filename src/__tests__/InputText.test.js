import React from 'react';
import {render, screen, fireEvent} from '@testing-library/react';
import InputText from '../components/InputText';
describe('InputTest Component', () => {
  test('renders button with value', () => {
   
  });
});
